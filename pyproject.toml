[tool.poetry]
name = "jaso"
version = "0.2.0"
description = ""
authors = ["Hansol-Lim <molmoty@gmail.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
watchdog = "^4.0.0"
rumps = "^0.4.0"

# 빌드 전용. macholib/modulegraph/altgraph는 py2app이 끌어온다.
[tool.poetry.group.dev.dependencies]
py2app = "^0.28.10"
setuptools = "^80.9.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
